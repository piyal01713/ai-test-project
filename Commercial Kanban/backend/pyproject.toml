[project]
name = "backend"
version = "0.1.0"
description = "FastAPI backend for Commercial Kanban"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn>=0.28.0",
]
readme = "README.md"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
